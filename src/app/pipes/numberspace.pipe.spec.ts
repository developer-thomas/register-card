import { NumberspacePipe } from './numberspace.pipe';

describe('NumberspacePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberspacePipe();
    expect(pipe).toBeTruthy();
  });
});
